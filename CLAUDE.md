# design-with-claude

## Project Overview
**V2 alpha live (April 2026):** subscription product that configures Claude Code for designers via an MCP server, paired with a browser companion that renders command outputs live.
- Live web: https://www.designwithclaude.com (start at `/start`)
- npm package: `designwithclaude@2.0.0-alpha.1`
- Persistence: Supabase (profiles + companion_events)
- See `PROGRESS.md` at the repo root for current state; `00-product-brief.md` + `0{1,2,3,4}-*.md` on Desktop for the canonical plan.

**V1 (still shipping):** 44 specialized design agents as Claude Code slash commands, available as a plugin or standalone. `commands/*.md` stays intact — the V2 MCP server reuses those files as role prompts.

## Architecture
- **V2 MCP server (root):** TypeScript, ESM, `@modelcontextprotocol/sdk` over stdio. Tools live in `src/tools/`. Shared types in `src/tools/types.ts`. Six structured output kinds: palette, type-scale, spacing, component-spec, copy, markdown.
- **V2 install flow:** `npx designwithclaude setup --token=imr_xxx [--scope=user|project]` writes the MCP entry. Uses `claude mcp add-json` when available, falls back to direct `~/.claude.json` edit with timestamped backup, or `.mcp.json` at the cwd for project scope.
- **V2 gating:** Pre-call `/api/gating/check`, post-call `/api/gating/consume`. Fails open when `DWC_GATING` is off (dev/alpha default).
- **V2 events:** Post-call emit to `/api/events`. Payload shape defined in `src/api-client.ts` → `EventPayload`. The browser companion (Phase 2) subscribes via Supabase Realtime.
- **V1 plugin (unchanged):** `.claude-plugin/plugin.json` manifest; `commands/*.md` are the knowledge base for both V1 slash commands and V2 MCP tool role prompts. `/design-brief` is the master command.

## Key Files
- `package.json` (root) — npm package `designwithclaude`, bins: `designwithclaude` (setup CLI) + `dwc-mcp-server` (server entry)
- `src/server.ts` — MCP server main; registers all tools; wraps handlers with gating + event emission
- `src/tools/index.ts` — tool registry
- `src/tools/types.ts` — `ToolDefinition`, `ToolResult`, output payload types
- `src/tools/loadPrompt.ts` — reads and caches `commands/*.md` role prompts
- `src/bin/setup.ts` — `npx designwithclaude setup` install flow
- `scripts/test-mcp-handshake.mjs` — stdio round-trip against `dist/server.js` (`npm run test:mcp`)
- `scripts/test-setup-dry-run.mjs` — install/uninstall against a tmp project (`npm run test:setup`)
- `.claude-plugin/plugin.json` — V1 plugin manifest (unchanged)
- `.claude-plugin/marketplace.json` — V1 marketplace catalog (unchanged)
- `commands/*.md` — role prompts, shared by V1 slash commands and V2 MCP tools
- `README.md` — V1 install + command reference (still current for the plugin path)
- `web/` — landing page; will become the V2 browser companion (Phase 2)
- `web/lib/dwc/{types,tokens,store}.ts` — shared types + in-memory store (Symbol.for singleton survives HMR) that Phase 3 will port to Supabase
- `web/app/api/tokens/validate`, `web/app/api/gating/{check,consume}`, `web/app/api/events`, `web/app/api/events/recent` — dwc API stubs consumed by the MCP server and the Phase 2 companion
- `web/app/api/profile/route.ts` — POST mints `imr_` tokens + stores onboarding answers + returns generated CLAUDE.md; GET returns stored profile
- `web/app/{start,profile,install,companion,upgrade}/page.tsx` — V2 companion flow pages (5 screens)
- `web/components/companion/` — `Shell`, `CopyButton`, `StartWizard`, `CompanionView`, plus `renderers/{Palette,TypeScale,Spacing,ComponentSpec,Copy,Markdown}Renderer.tsx` + barrel `renderers/index.tsx`
- `web/lib/claude-md-generator.ts` — extended with `tone_preference` for the V2 flow
- `web/lib/dwc/supabase.ts` — lazy service_role Supabase client (server-side only; RLS blocks anon)
- `web/lib/dwc/store.ts` — async store; dispatches to Supabase when `DWC_SUPABASE_URL` + `DWC_SUPABASE_SERVICE_ROLE_KEY` are set, in-memory Map fallback otherwise
- `web/supabase/{schema.sql,SETUP.md}` — schema + 5-step user setup doc for Supabase project + Vercel env

## Command File Structure
Each command follows this format:
```
---
description: Short description for Claude Code's command picker
---

Role statement with $ARGUMENTS placeholder

## Expertise
## Design Principles
## Guidelines
## Checklist
## Anti-patterns
## How to respond
## What to ask if unclear
```

## Naming Convention
Commands use pure role-based names (e.g., `accessibility-specialist`, `motion-designer`, `form-designer`). No `design-` prefix except for `design-brief` (the master command) and `design-system-architect`.

## Recent Sessions

### Session 2026-04-22 evening (MacBook)
- **Pattern:** Shipped alpha.3 working tree — `dwic audit` CLI. Zero-friction one-liner that scans a project, prints an 8-category dashboard, writes a shareable markdown report, pings anonymous telemetry so we can measure CLI → MCP funnel.
- **Status:** Code-complete on `audit-cli` branch. 20 test suites green. Publish + screencast pending.
- **Files Changed:** 15 new (src/bin/audit.ts, src/audit/{args,walker,aggregator,dashboard,markdown-report,telemetry}.ts, examples/broken-project/{package.json,src/styles/themes.css,src/styles/motion.css,src/components/Signup.tsx}, ROADMAP_AUDIT_CLI.md, 5 test scripts), 3 edited (src/bin/setup.ts for subcommand dispatch, package.json bin+scripts+version, README.md top-level CTA, PROGRESS.md + CLAUDE.md session notes).
- **Tests Added/Modified:** 5 new — test:audit-walker (8), test:audit-aggregator (16), test:audit-dashboard (12), test:audit-markdown-report (7), test:audit-cli-e2e (~8). Full regression sweep passing (all 20 suites).
- **Notes:** Kicked off with the advisor frame from user: "pick the single strongest skill — the auditor framing — and turn it into a real tool with a demo." Planned in plan mode, executed in auto mode. **Architecture bet:** the CLI and the MCP share the same audit brain — every `runXAudit()` is already a pure function (the whole reason we factored helpers out of the tool wrappers in batches 2 + 3). The CLI is ~350 new lines of composition on top of existing helpers. **Walker design:** sync walker with skip-set (`node_modules`/`.next`/`dist`/`.git`/etc.), prioritises high-signal CSS files (themes.css, tokens.css) so token parsers see the designer's canonical surface ahead of framework preflight. JSX/TSX files pre-filtered by regex for opening elements — keeps hooks/utils out of a11y audit noise. Cap at 200 markup files with cap-notice in the dashboard. **Dashboard rules:** hand-rolled ANSI (no `chalk` dep — keeps deps flat), respects NO_COLOR + non-TTY, severity-sorted rows (errors first, clean last), up to 3 MCP follow-up suggestions grounded in actual findings. Exit code mirrors severity (0/1/2) for CI gates. **Telemetry contract:** first-run opt-out notice printed once (stored in `~/.dwic/state.json::telemetryNoticeShown`), anonymous client ID minted via `crypto.randomBytes(6).toString('base64url')`, payload is schema-versioned (`dwic.audit.summary/1`) and PII-free (framework + category counts + totals only). `--no-telemetry` + `DWIC_TELEMETRY=off` both disable. 2s timeout, silent failure — never blocks exit. **Two bugs caught during smoke:** (1) dashboard header used `process.cwd()` instead of the `--cwd=` arg; fixed by threading `cwd` through DashboardOptions. (2) form regex `/\b<(form|input|...)\b/i` didn't match because `\b` before `<` fails when the preceding char is also non-word; dropped the leading `\b` and the fixture's form findings surfaced correctly. **Fixture design:** `examples/broken-project/` is a real Next.js 15 + Tailwind v4 project with planted findings in every audit category. Output: 28 findings, 6 errors, 14 warns, 8 info, exit 2. Same fixture powers the e2e test AND the screencast target. **Open:** npm publish needs a fresh automation token per the rotation policy (revoked yesterday); user to cut the 60s screencast before distribution. **Decision point baked into the plan:** after 7 days of launch telemetry, either double down on the auditor thesis or reassess if the 45-skills library framing was right all along.

### Session 2026-04-22 (MacBook)
- **Pattern:** Rename cleanup + alpha.2 — audit batch 3 + three Cognition-roadmap items (C9 slice 3 + C10 slice 2 + C1) in one publish.
- **Status:** Code-complete in working tree. 17 test suites green. Publish pending user sign-off.
- **Files Changed:** 18 — 6 modified (package.json, src/server.ts, src/designer.ts, src/tools/index.ts, scripts/test-memory-context.mjs, scripts/test-explicit-asks.mjs, PROGRESS.md, CLAUDE.md), 10 new (src/tools/{content,content-strategist,motion,motion-designer,design-next-step}.ts, src/utils/detect-project-config.ts, commands/design-next-step.md, scripts/test-audit-{content-strategist,motion-designer}.mjs, scripts/test-{detect-project-config,design-next-step}.mjs).
- **Tests Added/Modified:** 4 new test scripts — test:audit-content-strategist (17), test:audit-motion-designer (18), test:detect-project-config (21), test:design-next-step (15). test:memory-context extended by 7 assertions. test:explicit-asks extended with fixtures for the three new tools.
- **Notes:** Day opened with rename cleanup — verified `npx @imrandwc/dwic@latest help` from clean tmp dir (✓), then revoked the npm automation token on the web and cleared it from `~/.npmrc` (both checklist items on PROGRESS.md ticked). Then user chose to keep building before committing. **Audit batch 3:** content-strategist (copy heuristics: weak CTAs, jargon/filler list, passive voice, >30-word sentences, ALL CAPS shouting outside known acronyms, Title Case headings) + motion-designer (CSS motion: missing reduced-motion → **error**, duration bounds >1000ms/<80ms, `transition: all`, animated layout props like top/width force reflow, infinite animations without guard → warn else info, majority-linear easing). Both follow the established batch-2 pattern (`<name>.ts` helpers + `<name>-specialist/designer.ts` tool definition + composeRolePrompt audit-mode closing instruction that forbids fresh generation). **C9 slice 3 (auto-detect):** green-field utility — no existing filesystem reading. `detectProjectConfig(cwd)` parses `package.json` (framework via dependency priority: Next → Remix → Astro → Nuxt → SvelteKit → Expo → RN → Vite), walks common CSS locations for `--color-*` + `--space-*` tokens, sanitizes `package.json.name` into a slug candidate, returns `{ framework, techStack[], designSystemHints[], slug, confidence: "high|partial|none" }`. Wired into server.ts main(): runs once on boot (not per-tool-call) and threads into `registerTool` ctx. `renderOnboardingResponse` now accepts the detection and prepends a "We detected..." block above the 6 questions. Fail-open by design — any file-read error drops to confidence `none` and the response falls back to the original 6 questions. **C10 slice 2 (richer gists):** `describeEvent` in designer.ts expanded to mine payload data — palette audit mode compares `input.accent` against parsed tokens' hex to report `mandated #1F3B90 present|MISSING`, markdown events parse the `Flagged N error(s), M warning(s), P info note(s)` line every audit renderer emits (yields `(audit) 3 error(s), 2 warn(s), 1 info` as a real gist), every gist capped at 120 chars with ellipsis. Zero payload-shape changes, zero server-side work — all inferred from existing `output.data` + `input`. **C1 (design-next-step):** new MCP tool that synthesizes profile + recent-events into a single named recommendation. `src/tools/design-next-step.ts` is ~60 lines — iterates `tools[]` for the roster (minus self/hello-world/set-project-profile), composes a role prompt from new `commands/design-next-step.md`. Optional `focus` input biases recommendation. Role prompt mandates ONE specialist, exact MCP tool call format, "Why not something else" falsifiability clause. Handler accepts empty input — designer just calls `design-next-step()` with nothing. **test:explicit-asks plumbing update:** needed a SKIP_BRIEF_ECHO set because design-next-step has no `brief` field; the C3 directive + response-shape check still applies, just not the brief-restatement sanity check. **No decisions left hanging** — all three items shipped end-to-end with tests; next action is commit + publish dance.

### Session 2026-04-21 afternoon (MacBook)
- **Pattern:** Product rename `designwithclaude` → **dwic** (d + wi + c = design with claude, pronounced "dwick"). Full internal rebrand — npm package, version, env vars, CSS classes, component/file names, binaries, logger prefix, user-facing copy, metadata, README. Domain stays `designwithclaude.com` (IBM pattern: short brand, long-form domain as explainer).
- **Status:** Complete. Published `@imrandwc/dwic@1.0.0-alpha.1` to npm; deprecated `designwithclaude@{2.0.0-alpha.1..5}` with pointer. Commits `60d1e48` + `e9f249f` pushed to main.
- **Files Changed:** 69 across two commits (60 + 9).
- **Tests Added/Modified:** 0 new; 8 existing scripts updated to use DWIC_* env vars. All 11 MCP suites + setup-dry-run + web production build green post-rename.
- **Notes:** Triggered by a naming question mid-session: "what do you think about renaming the product to dwic?" Initial response was conservative (minimal public-copy-only swap) — user pushed back with "we don't have any users" and chose full internal rename. Planning saga: three scoping questions (version / repo rename / old-package handling), user rejected structured questions to clarify the domain-vs-brand tension first, landed on **Option 1: keep designwithclaude.com as the long-form brand explainer**, rename everything else. **Execution (~50 minutes):** (1) package.json rename + version reset to 1.0.0-alpha.1, (2) `DwcConfig` type → `DwicConfig` + DWC_* env vars → DWIC_* across src/, (3) CSS find-replace for ~150 `.dwc-*` classes, (4) file/dir renames via `git mv`: `DwcIcon.tsx` → `DwicIcon.tsx`, `web/lib/dwc/` → `web/lib/dwic/`, `dwc-icon.svg` → `dwic-icon.svg` + ~25 import-path updates, (5) user-facing copy + metadata across Hero, AuditDemo, InstallV2, account, install, layout.tsx, manifest.json, library cross-link, README. **Publish curveball:** npm rejected unscoped `dwic` as "too similar to swig/twig" — pivoted to scoped `@imrandwc/dwic` (install command becomes `npx @imrandwc/dwic setup …`, slightly longer but matches @anthropic-ai/*, @vercel/* convention). **Test fallout:** test-onboarding-gate failed after env var rename (test script still set `DWC_*`); fix was mechanical find-replace in the script. All 11 suites green after. **Propagation lag:** first-time scope publishes on npm have a 5-15min read-side delay; `npm view @imrandwc/dwic` 404s for a window even though the write-side accepted (proven by a re-publish attempt 403'ing with "cannot publish over previously published versions"). **Memory-worthy:** user's "we don't have any users" framing flipped a conservative migration plan (minimal scope) into a full rename in under an hour. Same principle already saved as `feedback_dwc_batch_ships_no_users.md` — this session is a second data point showing it applies to any cost-tradeoff decision, not just batch-shipping.

### Session 2026-04-21 13:52 (MacBook)
- **Pattern:** Interactive AuditDemo rebuild + V2 landing repositioning. Built an interactive audit demo with client-side regex audit helpers mirrored from the MCP server, a state-machine-driven reveal animation (parsing → computing → revealing → complete), and "production view" components that replace the code input with rendered mini product cards (Aperture) annotated with numbered pins + a legend. Shipped the V2 landing rebuild moving V1 skills to /library and repositioning hero as "design auditor, inside Claude Code".
- **Status:** Complete — live at https://www.designwithclaude.com (pending Vercel redeploy). `designwithclaude@2.0.0-alpha.5` already published separately earlier in the day.
- **Files Changed:** 6 in this save — 3 modified (web/app/globals.css, web/app/skills.css, web/components/skills/AuditDemo.tsx) + 3 new (web/lib/audit/{color,accessibility,form}.ts). Earlier commits in same day on main: 99f36d9 (V2 landing skeleton), e4a8b48 (logo refresh), 4c524b0 (hero pivot), 78f69db (metadata sweep), 28f4c69 (subtitle fix), 6b5bccd (alpha.5 publish).
- **Tests Added/Modified:** 0 in this save (web-only polish; MCP audit tests are mirrored client-side so no regression risk on server audits). Session earlier in day added 55 assertions via test:audit-{accessibility,form-designer,navigation-specialist}.
- **Notes:** Session arc was heavy on UX iteration. Started with the "design auditor" positioning pivot (user noticed "paste your CSS" in hero was dishonest since dwc is an MCP server not a webapp — rewrote subtitle to "Install it in Claude Code"). Then tackled the product-layer question ("are all 45 skills auditing?" — honest answer: no, only 7 of 11 V2 tools). User picked "V2 is the product" over splitting the pitch, leading to the landing rebuild: `/` becomes pure V2 marketing with Hero + AuditDemo + InstallV2 + SpecialistsList; V1 SkillsDirectory moves to `/library` with its own hero and cross-link. **Interactive AuditDemo iterations:** (1) static-card demo → (2) added Run-audit button + state machine with 350/350/110ms stage timings → (3) split into Input/Output columns side-by-side → (4) swap left column from code-view to production-view after Run → (5) slowed to 1100/1100/420ms so progression feels real → (6) added phase-parsing / phase-scanned CSS class so annotations fade in DURING the stage progression not at the start → (7) visual redesign: replaced abstract swatch-rows with mini "Aperture" product cards using dwc's typography (Bevellier/Satoshi) on light + dark surfaces; replaced floating inline badges with numbered pins (①②③) in the top-right corner of annotated elements + matching legend below; rebuilt a11y/form previews as real signup cards with gradient hero, branded buttons, realistic form fields. **Dimension lock saga:** user reported width shrink on Run audit — three rounds of fixes. First thought it was grid re-layout → locked min-height. Then user clarified width was jumping — added scrollbar-gutter: stable to html + demo containers. Still happened on A11y/Form tabs specifically (Color was shorter content so no overflow) — traced to A11y/Form content sitting right at 700px with the scrollbar appearing mid-animation and eating 17px horizontally. Final fix: bumped `.dwc-demo-body` height to 780px (content headroom) + added scrollbar-gutter to the code-block (so idle-state and running-state reserve identical scrollbar space). **Client-side audit helpers:** `web/lib/audit/{color,accessibility,form}.ts` mirror the MCP server's audit functions so the demo runs the same regex + contrast math in the browser. The product claim "nothing mocked" holds: findings shown in the demo are computed, not hardcoded. Accessibility and form helpers were direct copies of `src/tools/{accessibility,form}.ts`; color helpers are a minimal subset (parseTokensFromCss, contrastRatio, classifyContrast).

### Session 2026-04-21 morning (MacBook)
- **Pattern:** alpha.5 — audit-rollout batch 2. Three new V2 MCP specialists with audit mode (accessibility-specialist, form-designer, navigation-specialist) so the hero's "design auditor" claim becomes substantively true (7/11 tools now audit, up from 4/9).
- **Status:** Code + tests green in working tree; publish pending.
- **Files Changed:** ~16 — 3 modified (package.json, src/server.ts version const, src/tools/index.ts, PROGRESS.md, CLAUDE.md, scripts/test-explicit-asks.mjs fixture extension, web/app/layout.tsx + manifest.json + Hero.tsx from earlier in same session), 9 new (src/tools/{accessibility,accessibility-specialist,form,form-designer,navigation,navigation-specialist}.ts, scripts/test-audit-{accessibility,form-designer,navigation-specialist}.mjs).
- **Tests Added/Modified:** 3 new test scripts totaling 55 assertions: `test:audit-accessibility` (23), `test:audit-form-designer` (17), `test:audit-navigation-specialist` (15). `test:explicit-asks` extended with 3 fixtures to cover the new specialists. All 11 suites green (total ~200 assertions).
- **Notes:** Trigger was a pointed user question — "are the 45 design skills actually auditing each specialty?" Honest answer: only 4 of 9 V2 tools did real audits, and 0 of 44 V1 slash commands do (they're markdown prompts, no server logic). User chose "Path B — broaden the audit rollout" over narrowing the hero claim. **Architecture pattern** reused from alpha.4 cleanly: each new tool has a `<name>.ts` helpers module (parsers + audit functions returning typed Finding[]) + `<name>-specialist.ts` (or `-designer.ts`) tool definition that composes findings into server-computed markdown and injects via composeRolePrompt with an audit-mode closing instruction that forbids fresh generation. **accessibility-specialist:** regex-based HTML/JSX parser (not a full AST walker) — flags missing alts, unlabeled form inputs via four-way labeling check (`<label for>`, wrapping `<label>`, aria-label, aria-labelledby), heading-level skips with "one finding is enough" noise-suppression, anchor-as-button, unnamed buttons, missing main/nav landmarks, skip link. Optional `foreground`+`background` hex inputs trigger server-side contrast computation reusing color.ts. Mode-split: audit returns regex findings + optional contrast; generate returns the V1 role prompt (`accessibility-specialist.md`). **form-designer:** audits form-specific patterns accessibility-specialist doesn't touch — inputs-outside-form wrapper, implicit input types (`text` default, missing `email`/`tel`/`number`), `*`-in-label without `required` attribute, password fields without reveal toggle (heuristic: regex for "show"/"reveal"/"visibility" in nearby markup), orphaned error/hint elements not wired via `aria-describedby` (id match), radio/checkbox groups unwrapped by `<fieldset>`, multiple submits in one form without `name` for server disambiguation. Regex-based fieldset detection properly handles both wrapped and unwrapped cases (false-positive guards tested explicitly). **navigation-specialist:** audits wayfinding patterns — multiple `<nav>`s require distinguishing aria-label, active-class-without-aria-current (screen readers don't infer "current" from CSS), missing skip links, `<ul>`/`<ol>` nesting ≥4 levels deep via open/close-tag depth counter, large navs (≥5 links) without `aria-expanded` button or hamburger/disclosure hint. **test:explicit-asks regression:** adding 3 tools broke the existing plumbing test because it iterates `tools[]` and required fixtures for each — extended the fixtures map with a11y/form/nav briefs loaded with explicit asks. All 11 suites green again. **Realized during session:** `navigation-specialist` tool name aligns with V1 filename (`navigation-specialist.md`) even though user said "navigation-designer" in the P2 list — V1 file naming wins for consistency with other tools like color-specialist.

### Session 2026-04-20 21:21 (MacBook)
- **Pattern:** alpha.4 ship + logo/favicon refresh — response to Anthropic's Claude Design launch. Audit-mode rollout (typography + spacing + DSA), C10 slice 1 memory injection, V1 hero repositioning, palette+spark icon swap, npm publish.
- **Status:** Complete — `designwithclaude@2.0.0-alpha.4` live on npm under `latest`; commit `40f88ff` (alpha.4 batch) + logo refresh commit pushed to main.
- **Files Changed:** 19 — 11 modified (package.json, src/server.ts, src/api-client.ts, src/designer.ts, src/tools/{typography,spacing,design-system-architect}.ts, web/components/skills/Hero.tsx, web/components/DwcIcon.tsx, web/app/icon.svg, web/public/{favicon,favicon-light,favicon-transparent,dwc-icon}.svg, PROGRESS.md, CLAUDE.md), 7 new (src/tools/{typography,spacing,design-system}.ts, scripts/test-audit-{typography,spacing,dsa}.mjs, scripts/test-memory-context.mjs).
- **Tests Added/Modified:** 4 new fixture-based test scripts totaling ~67 assertions: `test:audit-typography` (21), `test:audit-spacing` (14), `test:audit-dsa` (16), `test:memory-context` (17). All prior tests (`test:mcp`, `test:explicit-asks`, `test:audit-mode`, `test:onboarding-gate`) still green.
- **Notes:** Trigger was Anthropic's April 20 launch of Claude Design (`claude.ai/design`), a visual canvas powered by Claude Opus 4.7 that generates prototypes, slides, landing pages with "automatic design system integration from codebase/files" and Claude Code handoff. Strategic read: the ideation-on-canvas surface is now commoditized by a first-party Anthropic tool; dwc hardens as the design-engineering rigor layer for code-first designers. User chose the "rigor + memory" lane over "contest the canvas". **C2 rollout:** typography / spacing / design-system-architect all gained `mode: "generate" | "audit"`. Parser patterns mirror `color.ts` — the same blockRe + declRe shape. Typography audit catches sub-12px mins, bad line-heights, non-standard weights, missing roles, mandated-font drift. Spacing audit infers base unit via GCD, flags off-grid values, step-jumps > 3×, missing common steps. DSA audit inspects STRUCTURE (primitive vs semantic layering, dark-scope theming, naming consistency, broken `var()` refs) rather than individual values — delegates value-level audits to the specialists. **C10 slice 1:** `api.fetchRecentEvents` calls the existing `GET /api/events/recent` endpoint; `src/server.ts` pre-flight refresh with 10s TTL cache (project-scoped via DWC_PROJECT_ID), kill-switch `DWC_MEMORY_CONTEXT=off`; `renderDesignerContext()` appends a "Recent design decisions" block after the existing profile section, most-recent-first, capped at 5 entries, with a one-line gist per event computed by `summarizeEvent()` off the event's output payload shape. **Hero repositioning:** user picked "Rigor-forward" direction via AskUserQuestion with preview; eyebrow → "Design-engineering rigor inside Claude Code", headline → "Audit, preserve, and persist your design system while Claude Code ships", subtitle ends "Pair it with any canvas tool — or bring your own system" to explicitly not contest Claude Design. **Logo swap:** replaced the old Claude-hand + full-palette-with-swatches icon with a single-path palette+spark from `~/Downloads/dwic.svg`. Single compound path with `fill-rule="evenodd"` so the 4-point spark cuts cleanly through the palette blob. DwcIcon.tsx switched from viewBox 59×57 to 24×24 (1:1), keeps `currentColor` so Nav/Shell inherit via CSS. All 4 favicon variants (dark, light, transparent, app-router `icon.svg`) + `dwc-icon.svg` updated with 4px padding inside a 32×32 viewBox. PNG variants (favicon-16/32, apple-touch, icon-192/512, favicon.ico) NOT regenerated — no rasterizer on the machine; user to run `rsvg-convert` / `magick` after `brew install librsvg imagemagick`. **Publish dance:** `npm login` established session but CLI publish still demanded OTP (again — matches April 17 notes verbatim). User pasted a Classic Automation token (`npm_dQB...`); setting it via `npm config set //registry.npmjs.org/:_authToken=...` bypassed OTP and `npm publish --tag=latest` succeeded. Smoke-tested `npx designwithclaude@latest help` from a clean tmp dir — clean usage output, registry confirms `2.0.0-alpha.4`. **Token hygiene:** Classic Automation token still active in `~/.npmrc` — user should revoke at npmjs.com per the April 14 pattern.

### Session 2026-04-17 22:10 (MacBook)
- **Pattern:** Cognition-roadmap P0 batch — C3 explicit-ask directive + C2 audit mode + C9 onboarding gate, shipped as alpha.3
- **Status:** Complete — `designwithclaude@2.0.0-alpha.3` published to npm under `latest`; commit `abcc1e6` pushed to main
- **Files Changed:** 14 (+1871 / -28) — 8 modified (package.json, src/api-client.ts, src/bin/setup.ts, src/server.ts, src/tools/{color,color-specialist,loadPrompt}.ts, PROGRESS.md), 6 new (FIELD_NOTES_COGNITION.md, ROADMAP_FROM_COGNITION.md, src/tools/set-project-profile.ts, scripts/test-{explicit-asks,audit-mode,onboarding-gate}.mjs)
- **Tests Added/Modified:** 3 new fixture-based test scripts totaling 38 assertions: `test:explicit-asks` (9), `test:audit-mode` (19), `test:onboarding-gate` (10). All prior tests (`test:mcp`) still green.
- **Notes:** Drove the first four items of `ROADMAP_FROM_COGNITION.md` in a single batch. **C3:** every specialist prompt now opens with an explicit-ask directive (centralized in `composeRolePrompt`); Cognition qualitative replay confirmed Claude leads with "Explicit asks from the brief", computes contrast ratios by name, answers before the default output. **C2:** `color-specialist` gained `mode: "generate" | "audit"` + `existingTokensCss` + `surfaces`; generate mode pins primary-500 to the exact mandated accent hex (fixes the silent `#1F3B90 → #2d56d2` drift from field notes), ships server-computed WCAG contrast on every token against `#FFFFFF` and `#121212`; audit mode parses `--color-*` declarations from CSS, computes a contrast matrix, flags failing pairings, detects structural gaps, confirms mandated-accent presence — no fresh seed emitted in audit mode. **C9 slice 1:** onboarding gate in `src/server.ts` fires when a token is present but the project has no onboarding answers; returns a structured response with 6 questions; new `set-project-profile` factory-pattern MCP tool persists answers via `POST /api/profile` and updates the in-process cache via `setDesignerProfile` so the same session releases the gate without restart. Gate runs BEFORE `gatingCheck` so onboarding doesn't burn free-tier slots. Exempts `hello-world` + `set-project-profile`. Kill-switch `DWC_ONBOARDING_GATE=off`; auto-disabled when no token. Slice 2 (prompt injection) lands for free via existing `renderDesignerContext()`. **Publish dance:** npm login via passkey worked, but CLI publish still demanded OTP; granular token failed under account's current 2FA mode; Classic Automation token succeeded (matches April 14 notes exactly). Decision to batch C3+C2+C9 into one alpha was driven by the "no users" framing — saved to memory as `feedback_dwc_batch_ships_no_users.md`.

### Session 2026-04-14 18:00 (MacBook)
- **Pattern:** Multi-project alpha.2 — schema + API + MCP + CLI + Web UX + ship
- **Status:** Complete — `designwithclaude@2.0.0-alpha.2` published; prod smoke test confirms two projects under one token with isolated profiles and events
- **Files Changed:** ~50 across schema migration, web API + store, MCP server + setup CLI, Web UX (new `/account`, new components: `AddProjectTile`, `SampleProjectCard`, `WelcomeBackBanner`)
- **Tests Added/Modified:** `test:phase2` assertions updated for new account/scoped API shapes; `test:setup` exercises `--project=<slug>` path
- **Notes:** Introduced projects as first-class under a single token (Option B from the design discussion). New `projects` + `project_profiles` tables via `web/supabase/migrations/001_projects.sql`; `companion_events` gained a `project_id` FK. `increment_command_count(text, uuid)` RPC bumps per-designer (canonical gate) AND per-project (informational) counters atomically. MCP server reads `DWC_PROJECT_ID`; setup CLI adds required `--project=<slug>` flag and defaults `--scope=project`. Web adds `/account` dashboard with sample/orientation card, inline `+` add-project tile, and demoted summary bar. StartWizard step-1 Back exits to `/account` instead of dying. `WelcomeBackBanner` saves the token in localStorage and nudges returning designers back to their account. Free-tier gating is per-designer (10 total) — simple upgrade story. `DWC_ALLOW_IMPLICIT_PROJECT` flag defaults on for alpha grace period; flip off once all alpha.1 installs upgrade. Plain-language `TESTING.md` written for future session handoff.

### Session 2026-04-14 15:30 (MacBook)
- **Pattern:** Ship alpha — Supabase persistence + npm publish
- **Status:** Complete — alpha is live
- **Files Changed:** 14 (Supabase client + SQL schema + SETUP doc + store refactor + 8 callers awaited); commits c955906 (gitignore .clerk/), 4b23587 (Supabase persistence)
- **Tests Added/Modified:** 0 (existing test:phase2 still passes against in-memory fallback)
- **Notes:** Diagnosed serverless memory loss (POST /api/profile → immediate GET = not_found because Vercel instances don't share memory). Added `web/lib/dwc/supabase.ts` lazy client using service_role key. Rewrote `store.ts` to async Supabase calls with in-memory fallback when env vars are unset. User created Supabase project, ran `web/supabase/schema.sql`, set `DWC_SUPABASE_URL` + `DWC_SUPABASE_SERVICE_ROLE_KEY` on Vercel, redeployed. Smoke test confirms profiles + events persist across serverless instances; gating counter increments atomically via `increment_command_count` RPC. Published `designwithclaude@2.0.0-alpha.1` to npm under `latest` tag — first publish required automation token (2FA-bypass granular token) because passkey 2FA + CLI publish still demands OTP even with `auth-only` setting. Token revoked after publish. Shareable URL: https://www.designwithclaude.com/start.

### Session 2026-04-13 23:00 (MacBook)
- **Pattern:** V2 Phase 2 browser companion — end-to-end
- **Status:** Complete — full loop (onboarding → token → install → MCP tool call → live render) works
- **Files Changed:** 18 new files in web/ (5 routes, 1 new API route, Shell + CopyButton + StartWizard + CompanionView components, 6 renderer components), lib/claude-md-generator.ts extended with tone_preference, 3 lib/dwc/* extensions, 1 new test script
- **Tests Added/Modified:** 1 (test:phase2 — posts /api/profile, drives MCP with minted token, validates events match ToolOutputPayload shapes, curls all 5 routes for server-rendered content)
- **Notes:** Built `/start` as a 5-step wizard (chips + textarea, not chat), `/profile` with meta tile + CLAUDE.md preview + copy button, `/install` with prefilled npx command, `/companion` with 2.5s polling of /api/events/recent + waiting/ready/feed states + "built so far" sidebar, `/upgrade` with retention-thesis copy + plan cards. 6 render components — PaletteRenderer (color grid with group tags), TypeScaleRenderer (ramp with live preview text), SpacingRenderer (horizontal bars), ComponentSpecRenderer, CopyRenderer, MarkdownRenderer (react-markdown + remark-gfm). New `POST /api/profile` mints imr_ tokens via `crypto.randomBytes(9).toString('base64url')`, generates CLAUDE.md via existing generator, stores onboarding answers. Polling chosen over Supabase Realtime for alpha — swap is Phase 3 work. Existing `/` landing page + `(app)/` routes intentionally untouched.

### Session 2026-04-13 21:30 (MacBook)
