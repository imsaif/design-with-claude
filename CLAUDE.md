# design-with-claude

## Project Overview
**V2 alpha live (April 2026):** subscription product that configures Claude Code for designers via an MCP server, paired with a browser companion that renders command outputs live.
- Live web: https://www.designwithclaude.com (start at `/start`)
- npm package: `designwithclaude@2.0.0-alpha.1`
- Persistence: Supabase (profiles + companion_events)
- See `PROGRESS.md` at the repo root for current state; `00-product-brief.md` + `0{1,2,3,4}-*.md` on Desktop for the canonical plan.

**V1 (still shipping):** 44 specialized design agents as Claude Code slash commands, available as a plugin or standalone. `commands/*.md` stays intact — the V2 MCP server reuses those files as role prompts.

## Architecture
- **V2 MCP server (root):** TypeScript, ESM, `@modelcontextprotocol/sdk` over stdio. Tools live in `src/tools/`. Shared types in `src/tools/types.ts`. Six structured output kinds: palette, type-scale, spacing, component-spec, copy, markdown.
- **V2 install flow:** `npx designwithclaude setup --token=imr_xxx [--scope=user|project]` writes the MCP entry. Uses `claude mcp add-json` when available, falls back to direct `~/.claude.json` edit with timestamped backup, or `.mcp.json` at the cwd for project scope.
- **V2 gating:** Pre-call `/api/gating/check`, post-call `/api/gating/consume`. Fails open when `DWC_GATING` is off (dev/alpha default).
- **V2 events:** Post-call emit to `/api/events`. Payload shape defined in `src/api-client.ts` → `EventPayload`. The browser companion (Phase 2) subscribes via Supabase Realtime.
- **V1 plugin (unchanged):** `.claude-plugin/plugin.json` manifest; `commands/*.md` are the knowledge base for both V1 slash commands and V2 MCP tool role prompts. `/design-brief` is the master command.

## Key Files
- `package.json` (root) — npm package `designwithclaude`, bins: `designwithclaude` (setup CLI) + `dwc-mcp-server` (server entry)
- `src/server.ts` — MCP server main; registers all tools; wraps handlers with gating + event emission
- `src/tools/index.ts` — tool registry
- `src/tools/types.ts` — `ToolDefinition`, `ToolResult`, output payload types
- `src/tools/loadPrompt.ts` — reads and caches `commands/*.md` role prompts
- `src/bin/setup.ts` — `npx designwithclaude setup` install flow
- `scripts/test-mcp-handshake.mjs` — stdio round-trip against `dist/server.js` (`npm run test:mcp`)
- `scripts/test-setup-dry-run.mjs` — install/uninstall against a tmp project (`npm run test:setup`)
- `.claude-plugin/plugin.json` — V1 plugin manifest (unchanged)
- `.claude-plugin/marketplace.json` — V1 marketplace catalog (unchanged)
- `commands/*.md` — role prompts, shared by V1 slash commands and V2 MCP tools
- `README.md` — V1 install + command reference (still current for the plugin path)
- `web/` — landing page; will become the V2 browser companion (Phase 2)
- `web/lib/dwc/{types,tokens,store}.ts` — shared types + in-memory store (Symbol.for singleton survives HMR) that Phase 3 will port to Supabase
- `web/app/api/tokens/validate`, `web/app/api/gating/{check,consume}`, `web/app/api/events`, `web/app/api/events/recent` — dwc API stubs consumed by the MCP server and the Phase 2 companion
- `web/app/api/profile/route.ts` — POST mints `imr_` tokens + stores onboarding answers + returns generated CLAUDE.md; GET returns stored profile
- `web/app/{start,profile,install,companion,upgrade}/page.tsx` — V2 companion flow pages (5 screens)
- `web/components/companion/` — `Shell`, `CopyButton`, `StartWizard`, `CompanionView`, plus `renderers/{Palette,TypeScale,Spacing,ComponentSpec,Copy,Markdown}Renderer.tsx` + barrel `renderers/index.tsx`
- `web/lib/claude-md-generator.ts` — extended with `tone_preference` for the V2 flow
- `web/lib/dwc/supabase.ts` — lazy service_role Supabase client (server-side only; RLS blocks anon)
- `web/lib/dwc/store.ts` — async store; dispatches to Supabase when `DWC_SUPABASE_URL` + `DWC_SUPABASE_SERVICE_ROLE_KEY` are set, in-memory Map fallback otherwise
- `web/supabase/{schema.sql,SETUP.md}` — schema + 5-step user setup doc for Supabase project + Vercel env

## Command File Structure
Each command follows this format:
```
---
description: Short description for Claude Code's command picker
---

Role statement with $ARGUMENTS placeholder

## Expertise
## Design Principles
## Guidelines
## Checklist
## Anti-patterns
## How to respond
## What to ask if unclear
```

## Naming Convention
Commands use pure role-based names (e.g., `accessibility-specialist`, `motion-designer`, `form-designer`). No `design-` prefix except for `design-brief` (the master command) and `design-system-architect`.

## Recent Sessions

### Session 2026-05-05 12:20 (MacBook)
- **Pattern:** SEO + search-engine submission prep — refresh `llms.txt`, tighten `robots.ts`, expand JSON-LD, wire Microsoft Clarity, plan submission to GSC + Bing.
- **Status:** Shipped. Typecheck clean (`npx tsc --noEmit` from `web/`). No new tests — all changes are metadata/static content.
- **Files Changed:** 3 modified — `web/public/llms.txt`, `web/app/robots.ts`, `web/app/layout.tsx`.
- **Tests Added/Modified:** 0.
- **Notes:** Plan-mode session that evolved into a small targeted SEO push. **Audit baseline:** site already had decent SEO foundations — full Metadata export with OG/Twitter, `SoftwareApplication` JSON-LD, `sitemap.ts` + `robots.ts`, manifest, and the GSC verification HTML (`googlea294ae68308b5c2c.html`) already shipped in `public/`. Real gaps: stale `llms.txt`, no analytics, single JSON-LD schema, robots didn't exclude admin/api. **Decisions captured:** canonical = non-www (`https://designwithclaude.com`); analytics = Microsoft Clarity only (skipped Vercel Speed Insights); refresh AI-discoverability surface (already shipped `designwithclaude.gist.design` in `public/`). **Edits.** (1) `web/public/llms.txt` — full rewrite: `/start` → `/get-started` (route deleted Apr 27), dropped "5-step wizard" framing for the post-Apr-27 email→token→install flow, set library count to **41 skills / 8 categories** sourced from `app/data/skills.ts` (verified by `node --input-type=module -e "import('...').then(m=>console.log(m.SKILLS.length, m.CATEGORIES.length))"` returning `41 8`), added pointer to `/designwithclaude.gist.design`, mentioned `--watch` flag. (2) `web/app/robots.ts` — converted single-rule to array form, added `disallow: ["/admin", "/admin/*", "/api/*"]` to save crawl budget (admin already had `noindex,nofollow` metadata, but robots-level disallow signals intent). (3) `web/app/layout.tsx` — imported `Script` from `next/script`; added second JSON-LD schema (`WebSite` with `alternateName: "dwic"`) alongside existing `SoftwareApplication` so Google can register brand + canonical URL distinctly from product; wired Microsoft Clarity via env-gated `NEXT_PUBLIC_CLARITY_PROJECT_ID` with `strategy="afterInteractive"` (no-ops cleanly when env var is missing — matches aiex pattern). **Flagged but not fixed:** `web/public/og-image.png` is referenced by metadata but doesn't exist anywhere in `public/` or as `app/opengraph-image.tsx` — social and search-card previews are silently broken. User to either drop a 1200×630 PNG or scaffold a dynamic `app/opengraph-image.tsx`. **Pending manual steps for user.** (a) Create Microsoft Clarity project at clarity.microsoft.com, set `NEXT_PUBLIC_CLARITY_PROJECT_ID` in Vercel prod env. (b) After deploy: GSC → Sitemaps → submit `https://designwithclaude.com/sitemap.xml` (verification already in place); skip Request Indexing per `feedback_gsc_request_indexing_unreliable.md`. (c) Bing Webmaster Tools → import GSC property → submit sitemap. (d) Verify Vercel www → non-www redirect in Settings → Domains. (e) Validate at `search.google.com/test/rich-results` post-deploy — should detect both `SoftwareApplication` + `WebSite` schemas. (f) Off-site distribution (out of scope to implement): `awesome-claude-code` + `awesome-mcp-servers` PRs, Anthropic Discord screencast post, r/ClaudeAI post on the auditor angle. **Auto-memory script glitch (4th occurrence in 6 days).** Same orphan bug as Apr 30 + May 1 morning + May 1 evening — script inserted "General updates / 3 files / 0 tests / 0" stub between the May 1 12:51 header and its body. Hand-repaired by collapsing the duplicate header and authoring real notes here. The pattern is now extremely consistent: feedback memory `feedback_save_script_ordering.md` + the May 1 12:51 entry both say a real script fix is overdue (detect last body line, not last header). Plan file for this session at `/Users/imranmohammed/.claude/plans/lets-submit-dwic-to-steady-grove.md`.

### Session 2026-05-01 12:51 (MacBook)
- **Pattern:** Homepage redesign — restructure hero around a single primary CTA. Make the CLI command (`npx @imrandwc/dwic audit`) the hero pill, morphing into an email field after Copy with a back-to-command escape hatch. Move alpha framing to a small badge next to the wordmark in nav. Replace one of the four explainer cards with a clickable free-library card. Expand the See-demo terminal to show all 8 audit categories with real findings + a totals line. Make the terminal traffic-light dots clickable to close the demo. Aborted attempt to migrate npm scope `@imrandwc/dwic` → `@dwic/dwic` after `dwic` turned out to be taken on npm.
- **Status:** Working tree shipped (typecheck clean, dev server live on :3100). 0 new tests this session — all changes are UI / styling.
- **Files Changed:** 4 modified — `package.json` (version bump 1.0.0-alpha.3 → 1.0.0-alpha.4), `web/components/skills/Nav.tsx` (alpha badge), `web/components/skills/GetStartedPreview.tsx` (massive rewrite: morphing pill, dot-cluster close button, 14-line demo, library card, primary-cli refactor), `web/app/skills.css` (+274 lines: gs-hero-cli pill, morph styles, terminal severity icons, library card hover, alpha badge styling).
- **Tests Added/Modified:** 0.
- **Notes:** Long iterative UI polish session driven by visual feedback on the live page. **(1) Visual hierarchy reset.** First proposal (CLI block as a small secondary CTA below email gate) was rejected — too many competing elements in the hero. Pivot: make the CLI command the PRIMARY pill, demote email gate as a secondary "Want Claude to fix what dwic finds?" follow-up. Second iteration still felt cluttered ("9 stacked elements competing"). Final move: pull email gate out of the hero entirely, move it to a closing section after the 4 cards. Hero becomes one-CTA: title, sub, navy CLI pill, trust strip, See demo. **(2) Morphing pill.** User suggested converting the CLI pill into an email field after Copy, with back-to-command escape. Built two-phase state machine in GetStartedPreview: copyCliCommand triggers 1.2s "Copied" feedback then auto-transitions to email phase; backToCli reverses. Email phase inlines `/api/get-started/mint` POST + router push (extracted from EmailGate so styles unify with the dark navy pill). Auto-focus on email input on phase change (60ms delay so input has rendered). Closing email section then deleted as redundant — hero pill captures both paths. **(3) Trust strip evolution.** Started as "Free during alpha · No credit card · 41 specialists in the free library" → became "Free during alpha · No signup, no token" after positioning shift → eventually deleted entirely after `alpha` framing moved to a nav badge. Free-library link moved into a dedicated 4th card (clickable card-as-link replacing "Monitor continuously" since the latter was redundant with "Catch drift"). **(4) Alpha badge.** Sits next to the `designwithclaude` wordmark in nav. First pass was 11px too prominent; second pass dropped to 9px font with lighter bg/border. Carries the alpha framing globally instead of in-hero. **(5) Demo expansion.** Terminal demo originally showed only 2 categories (Color + Typography). Rebuilt with all 8 audit categories — Color (✗ error), Typography (⚠), Spacing (⚠), Accessibility (✗), Form (⚠), Navigation (·), Motion (·), Content (✓) — plus totals line "28 findings · 6 errors · 14 warns · 8 info (exit 2)" matching the actual `examples/broken-project` fixture output, plus a "→ Run color-specialist via MCP" suggestion. New CSS classes `.gs-terminal-ok`, `.gs-terminal-summary`, `.gs-terminal-divider`. Terminal body height bumped 320 → 420px to fit. **(6) Demo close interaction.** User wanted the macOS-style traffic-light dots to actually close the demo. First implementation made each dot a separate button (a11y mess: 3 buttons with same "Close demo" label); refactored to a single transparent wrapper button containing the three colored dots as decorative spans. Added a focus ring on the wrapper. Brief detour where I added a × icon inside the red dot on hover — user said "keep the dots as they were" — reverted to plain colored circles, but kept the click-to-close functionality. Standalone Close demo pill restyled to solid navy + drop shadow (was transparent ghost — too quiet). **(7) Demo content correction.** Demo was showing `dwic audit --watch` but the watch flag is in working tree only (Apr 27, never published — alpha.4 still pending). User caught the inconsistency. Updated demo first line to `npx @imrandwc/dwic audit` matching the hero CTA, dropped "drift" framing from caption (one-shot scan, not watch loop), restored when alpha.4 ships. **(8) Library card.** Replaced "Monitor continuously" card with a clickable BookOpenIcon card → "41 specialists, free" → "Browse the open library of design specialists you can drop into Claude Code today." → "Open library →". Whole card is a `<Link>` to `/library` with hover lift + border treatment via new `.gs-card--link` variant. Strategic call: kept "Catch drift on every run" because that's the unique auditor hook; "Monitor continuously" was the redundant card (essentially "catch drift but on CI"). **(9) npm scope rename — aborted.** User asked if their npm username `imrandwc` needs to be in the install command. I said no — pre-users, switching cost is zero (same logic as the Apr 21 designwithclaude → @imrandwc/dwic rename). Bulk find-replaced `@imrandwc/dwic` → `@dwic/dwic` across 13 live files, bumped version to alpha.4, both typechecks passed. Then user tried to register `@dwic` org on npm and got "username dwic is already taken" — someone else had reserved the name. Discussed alternatives (`@dwicdesign/dwic`, `@dwc/dwic`, `@designwithclaude/dwic`); recommendation: doubled-name awkwardness (e.g. `@dwicdesign/dwic`) isn't an upgrade over `@imrandwc/dwic`, just a different vanity wrapper. User said "ok"; reverted all 13 files back to `@imrandwc/dwic` via reverse find-replace. The `1.0.0-alpha.4` version bump stayed (still appropriate — we have surface trim + watcher + telemetry fix + homepage all queued for the next publish). **Pending after this push.** (a) Cut alpha.4 publish: surface trim (Apr 30) + `dwic audit --watch` (Apr 27) + telemetry fix (this morning) + homepage redesign + library card. Needs `npm login` + 2FA. (b) 60s screencast against `examples/broken-project` for distribution. (c) Day-7 measurement once distribution actually happens — current 7-day window had nothing to measure (broken pipe + no broadcast). (d) Run migration `002_email.sql` on production Supabase before `/admin/signups` is useful. (e) Auto-memory script glitch happened AGAIN — same as Apr 30 + this morning's earlier save. The script inserts the new session stub between the previous session's header and body, orphaning the prior entry. Hand-repaired all three times today. Worth a real fix (script should detect last header line and append after the last body line, not after the last header).

### Session 2026-05-01 11:15 (MacBook)
- **Pattern:** Telemetry pipe fix — anonymous CLI events on `/api/events`. Day-7 telemetry check for the `dwic audit` CLI surfaced that the events route was rejecting every CLI ping silently (`anon_*` clientId vs the route's `imr_*` token-shape gate); built a separate validation + storage path for allowlisted anonymous tools.
- **Status:** Shipped local, Vercel deploy pending the push that follows this save. 3/3 anonymous-events smoke assertions green; root typecheck clean; no other suites disturbed.
- **Files Changed:** 5 — 4 modified (`web/lib/dwic/tokens.ts`, `web/lib/dwic/store.ts`, `web/app/api/events/route.ts`, `package.json`) + 1 new (`scripts/test-anonymous-events.mjs`).
- **Tests Added/Modified:** +1 — `test:anonymous-events` (3 assertions: valid anon ping → 200/stored, anon token + non-allowlisted tool → `anonymous_tool_not_allowed`, bogus token → `invalid_token`).
- **Notes:** Started the session as a Day-7 decision checkpoint — `dwic audit` CLI shipped Apr 24 in alpha.3 with a 7-day measurement plan baked in (telemetry → dashboard → decide whether to double down on the auditor thesis or reassess the 45-skills framing). Queried Supabase for the past 7 days: **6 events total, all `__mcp.connected__`, 1 unique token_hash (≈me), 0 new profiles, 0 paid.** Two findings explained why the data was meaningless. (1) **Telemetry pipe broken since alpha.3 launch.** `src/audit/telemetry.ts:138` posts with `token: anon_<base64>` (anonymous client ID minted via `crypto.randomBytes(6).toString('base64url')`), but `web/app/api/events/route.ts:21` runs `isTokenShapeValid(token)` which only matches `^imr_[A-Za-z0-9_-]{8,}$` (`web/lib/dwic/tokens.ts:3`). Every CLI ping returned `{ok: false, reason: "invalid_token"}` HTTP 200 — and the CLI's `fetch()` is in a try/catch with silent failure (`telemetry.ts:154`), so neither side surfaced anything. (2) **No distribution happened.** PROGRESS.md still has the screencast + Discord/Reddit/X post checkboxes empty — alpha.3 published but never announced. So the 7-day window measured nothing. **Fix design.** Two options surfaced: widen the existing validator with a tool-name allowlist conditional, OR add a second route `/api/events/anonymous`. User picked option 1 once I confirmed they capture identical data; option 2's only edge was rate-limit isolation, not worth the file count pre-users. **Implementation.** `web/lib/dwic/tokens.ts` — added `ANONYMOUS_TOKEN_PATTERN = /^anon_[A-Za-z0-9_-]{6,}$/` and `isAnonymousTokenShapeValid()`, plus `ANONYMOUS_TOOL_ALLOWLIST: ReadonlySet<string>` currently containing only `__cli.audit.summary__`. `web/lib/dwic/store.ts` — added `recordAnonymousEvent({token, toolName, input, output, timestamp})` that writes to `companion_events` with `project_id: NULL`, **bypassing `ensureAccount()` and `resolveProject()`** so anonymous tokens never pollute `profiles` or `projects` tables (would have created one row per anonymous CLI user otherwise). Mem-fallback path stores in `events` map keyed by `__anon__:<hash>`. `web/app/api/events/route.ts` — branches on token shape: `anon_*` → allowlist check → `recordAnonymousEvent`; `imr_*` → existing `recordEvent` path; anything else → `invalid_token`. Response includes `anonymous: true` flag for observability. **Verified `companion_events.project_id` is nullable** (added in migration `001_projects.sql` with no NOT NULL constraint) before committing to the bypass design — wouldn't have worked if the column required a value. **Test design.** `scripts/test-anonymous-events.mjs` spins up `next dev -p 3457` against `web/.env.local` (real Supabase service role), waits up to 30s for the route to respond, fires three POSTs. The valid case actually wrote a row to **production Supabase** — cleaned up post-test with a one-shot DELETE script keyed by sha256(`anon_testaB3xK9zP`). One test row in prod for ~30 seconds, then gone. **Schema discovery side-quests.** First Supabase query failed because I assumed `created_at` on `companion_events` (it's `received_at`). Second query failed on `profiles.email` because migration `002_email.sql` hasn't run on production yet — pre-existing, flagged but not fixed in this session (the `web/api/admin/signups` route would already 503 if it tried to query email). **Pending after this push.** (a) Cut alpha.4 to ship surface trim + watcher (`dwic audit --watch` from Apr 27) + this telemetry fix together so `npx @imrandwc/dwic audit` users actually emit measurable pings. (b) Cut the 60-second screencast against `examples/broken-project` and post in Anthropic Discord. (c) Wait 7 days from THAT, then take the real Day-7 measurement. (d) Run migration `002_email.sql` on production Supabase if `/admin/signups` is going to be useful. **Auto-memory script glitch (recurrence).** Same bug as Apr 30 — `update-memory.sh` produced a "General updates / 5 files / 1 test" stub and inserted it between Apr 30's header and body line, orphaning the surface-trim entry. Hand-repaired by collapsing the duplicate header and authoring real notes for both. The feedback memory `feedback_save_script_ordering.md` already says the script needs to run before commit (it does here, since I'm following the skill workflow correctly), but the orphaning problem is separate — the script's insertion cursor lands between the most-recent header and its body when the most-recent entry was the last save's hand-edited notes. Worth a deeper script fix later.

### Session 2026-04-30 20:25 (MacBook)
- **Pattern:** Surface trim to auditor — drop design-next-step + 4 orphan commands; ship Apr 29 admin-dashboard expansion + audit color-role classifier in same push.
- **Status:** Shipped. Two commits pushed (`99feab2` admin/color-roles, `2df6aa9` surface trim). 20/20 test suites green, audit CLI exit 2 on broken-project fixture, web build clean.
- **Files Changed:** 35 across two commits — 20 in batch 1 (admin dashboard + aggregator), 15 in batch 2 (surface trim).
- **Tests Added/Modified:** +1 (`test:audit-color-roles`), -1 (`test:design-next-step` deleted with the tool).
- **Notes:** Strategic conversation that turned into a small surgical change. **The framing question.** User asked "should we start dwic as a design system auditor instead of a full promise" — I read the codebase first and surfaced that the auditor positioning was already in place since alpha.4 (Apr 20 hero rewrite, alpha.3 audit CLI). So the question wasn't pitch, it was build-surface bloat. User confirmed: driver is "build surface too wide." That reframed it from "rewrite copy" to "cut code." **What I expected vs. what I found.** Plan called for cutting the browser companion (`/companion` polling + 6 renderers + `Shell`) — Explore agent found Apr 27 cleanup had already deleted all of it, so that part of the plan was a no-op. The 4 non-audit specialists (empty-loading-states, icon-illustration-specialist, notification-designer, settings-designer) turned out to be **orphan markdown files only** — they had `commands/*.md` role prompts but no `src/tools/*.ts` implementations were ever built. Pure file deletes, zero blast radius. `design-next-step` was the only tool actually wired in: deleted the tool + role prompt + test, unwired from `src/tools/index.ts`, removed from `package.json` scripts and the `SKIP_BRIEF_ECHO` set in `test-explicit-asks.mjs`. **Stale URL sweep.** While I was in there, fixed `/start` → `/get-started` in the audit CLI install CTA (`src/audit/dashboard.ts`), `set-project-profile` fallback message, README (×2), and `web/app/sitemap.ts`. Dropped the broken `/upgrade` link from `web/app/api/gating/check/route.ts` error copy entirely — there's no payment route, sending users to a 404 is worse than no link. **Generate modes stay.** Explicit user decision: generate-mode on the audit-capable tools (color/typography/spacing/a11y/form/nav/motion/content/DSA) reinforces the loop (generate token → ship → audit drift). Same file, same prompt, two modes — cheap to keep. **Two-commit save.** When I ran `git status` for the save flow, found ~20 files of pre-existing uncommitted Apr 29 work in the tree: a substantial color-audit improvement (`src/audit/aggregator.ts` +75 lines adding a token-role classifier so `--background-*` / `--border-*` tokens don't get flagged as failing AA against themselves, plus dark-scope tokens pair against a dark surface) and admin dashboard expansion (`/admin` → `/admin/{signups,subscribers,events}` with shared `_ui.tsx` + `admin-nav.tsx` + `admin.module.css` + new API routes). Per the user's "don't `git add -A` when there's pre-existing work" memory rule, flagged this and got two-commit confirmation. Split via temporary `package.json` revert: committed Apr 29 batch first with `test:audit-color-roles` added, then re-edited package.json to drop `test:design-next-step` and committed the trim. **Auto-memory script glitch.** `~/.claude/scripts/update-memory.sh` reads from git status, which was clean post-push, so it produced a "0 files / 0 tests / General updates" stub and inserted it between the previous session's header and body — orphaned the Apr 28 19:56 header and left a stray literal `0` line. Hand-repaired the entry with real numbers + this note. Worth knowing: the script needs to run BEFORE the commit, not after, to capture diff stats. **Pending follow-ups.** (a) PROGRESS.md still says "9 / 14 V2 tools audit server-side" with `design-next-step` in the non-audit list — needs a one-line update post this trim; deferred so it doesn't bloat this commit. (b) The audit CLI's first-run-on-a-project gives exit 0 even when errors exist (because there's no baseline to drift from) — pre-existing behavior, but fixture run showed exit 0 where the original ROADMAP said exit 2. Not a regression I introduced; flag for later if the contract matters. (c) npm publish of `1.0.0-alpha.4` deferred — bundle this trim with the next functional alpha.

### Session 2026-04-28 19:56 (MacBook)
- **Pattern:** Admin dashboard for `/admin/signups` — view email submissions captured at `/get-started`
- **Status:** Shipped end-to-end. Typecheck clean. Dev server live on :3000 with Supabase wired.
- **Files Changed:** 6 new (`web/lib/admin-auth.ts`, `web/app/api/admin/login/route.ts`, `web/app/api/admin/signups/route.ts`, `web/app/admin/{layout,page,signups-client}.tsx`).
- **Tests Added/Modified:** 0.
- **Notes:** Ported aiex's admin auth pattern verbatim — HMAC-signed stateless session cookie (`dwic_admin_session`, 24h TTL), `timingSafeEqual` password compare against `ADMIN_PASSWORD` env var, in-memory IP rate limiter (5 attempts / 15min lockout) on the login route. **Auth lib (`lib/admin-auth.ts`):** `verifyAdminPassword`/`createSession`/`validateSession`/`checkAdminAuth` (server components) + `withAdminAuth<T>(handler)` higher-order wrapper for API routes. Secret key derives from `ADMIN_SESSION_SECRET` → falls back to `ADMIN_PASSWORD` itself → fallback string. Cookie is `httpOnly`, `sameSite: lax`, `secure` only in production. **Signups API (`api/admin/signups/route.ts`):** queries `profiles` rows where `email IS NOT NULL`, ordered by `created_at DESC`, with optional `?search=` ilike on email + `?status=free|paid|cancelled` filter + page/limit pagination (capped at 500/req). Returns `{signups, pagination, stats: {total, paid, free}}`. Stats use two parallel `head: true` count queries so they're cheap. Returns 503 with a clear message if `supabaseConfigured()` is false instead of crashing. Used `withAdminAuth<unknown>` explicit generic to silence TS inference complaint about the union return type. **UI (`admin/signups-client.tsx`):** login gate → table with email / status pill (paid=emerald, cancelled=red, free=slate) / command_count / created_at / last_seen_at, plus search box (debounced via Enter), status dropdown, CSV export (limit=10000 for export), Previous/Next pagination. Server component (`page.tsx`) calls `checkAdminAuth()` and passes `initialAuth` so authenticated users skip the login form on hard refresh. Layout adds an admin-scoped nav with "Back to site" link + `noindex,nofollow` metadata. **Env wiring during session:** set `ADMIN_PASSWORD="Aman@0829"` in `web/.env.local` to match aiex (`.env.local` is gitignored — confirmed via `git check-ignore`); user pasted production `DWIC_SUPABASE_URL` + `DWIC_SUPABASE_SERVICE_ROLE_KEY` directly in chat which I appended to `.env.local`. **Security flag raised:** the service-role key bypasses RLS — pasted creds now sit in conversation history. Recommended user rotate via Supabase dashboard → Settings → API → Reset service_role key, then update both Vercel and local `.env.local`. No automatic rotation performed. **Schema dependency:** the email column was added in `web/supabase/migrations/002_email.sql` (already shipped, indexes `email IS NOT NULL`); no new migration needed for the dashboard. **Outstanding follow-ups.** (a) Rotate the service-role key as noted above. (b) Set `ADMIN_PASSWORD` (and optionally `ADMIN_SESSION_SECRET`) in Vercel production env so `/admin` works on https://www.designwithclaude.com. (c) The dashboard is read-only — no toggle/delete actions yet. If we want a "delete account" or "mark as paid manually" flow, add PATCH/DELETE to `signups/route.ts` mirroring aiex's `subscribers` route. (d) Multiple lockfiles warning still showing in Next dev output (`/Users/imranmohammed/design-with-claude/web/package-lock.json` is being treated as workspace root) — pre-existing, not blocking, worth a separate cleanup. (e) Three failed attempts at `npm run dev` happened during the session because pkill killed the previous bg task before the env reload; final restart on :3000 succeeded with `Environments: .env.local` confirmation in the boot log.

### Session 2026-04-28 12:31 (MacBook)
- **Pattern:** Landing page (`/`) hero polish — dwic-letter highlight in nav wordmark, animated terminal demo with See-demo CTA, hero+cards layout overhaul.
- **Status:** Shipped end-to-end. Typecheck clean across all rounds. Footer dropped from `/` only (`/library` keeps it).
- **Files Changed:** 4 — modified (web/components/skills/{Nav,GetStartedPreview}.tsx, web/app/skills.css, web/app/page.tsx).
- **Tests Added/Modified:** 0.
- **Notes:** Long iterative polish session, ~15 distinct user-driven course-corrections. **(1) Nav wordmark.** Restructured `Nav.tsx` line 9 from `design<accent>with</accent>claude` (the existing accent was visually invisible because `--accent` resolves to the same value as `--text-primary`) into 6 spans where d / wi / c are full-strength and e-s-i-g-n / t-h / l-a-u-d-e are dimmed at 40% opacity — surfaces the dwic mnemonic per the April 21 internal rebrand. Added `aria-label="designwithclaude"` on the outer span + `aria-hidden` on the inner so screen readers still announce the long-form. **(2) Blinking cursor after "terminal"** in the hero H1 — reused the existing `.skills-hero-cursor` class (6px navy bar, 900ms blink, prefers-reduced-motion guard). **(3) Static terminal demo** added below the EmailGate showing `dwic audit --watch` catching a contrast error (primary-500 changed #1F3B90 → #2d56d2 fails AA on background-50, 3.8:1 needs 4.5:1) + a typography drift (body font 14px below mandated 16px). Real ANSI-style chrome + path + numeric times + warn/error coloring. **(4) Demo became opt-in.** User asked for a "▶ See demo" secondary CTA that swaps the entire hero content for the demo with a "✕ Close demo" pill; built it as a client-component state toggle with a useEffect-driven staged-reveal (each line appears with its own delay totaling ~5s, 220ms fade-in animation, prefers-reduced-motion guard). Added a "↻ Replay" pill once all lines finish. **(5) Trust strip** under the EmailGate — 3 bullets ("Free during alpha · No credit card · 48 specialists in the free library") with the third linking to /library; pulled count from `SKILLS.length` for sync. Honest framing per CLAUDE.md alpha state — no "free forever" claim. **(6) Spacing-specialist consult** via `mcp__designwithclaude__spacing-specialist` to fix the perceived "unbalanced hero." Returned a 4px-base scale; applied the tight-bond/separation rhythm rule (form gets primacy at 48px from sub; trust hugs form at 24px; demo CTA gets 32px daylight; symmetric 80px hero↔cards section break). **(7) Fit-in-viewport pass.** User wanted no scroll on a typical laptop; wrapped `.gs` in flex column with `min-height: calc(100svh - 8rem)` reserving nav+footer, switched all hero/cards padding to vh-clamps so they self-adjust on shorter viewports, tightened footer padding 32→20. **(8) Hierarchy refactor.** Made hero a tinted "stage" via `--background-grain`; demoted cards to a borderless icon strip on white below — but user pushed back. **(9) Footer dropped on `/` only.** Moved cards inside the hero `<section>` so they share the tinted stage, restored card surfaces (white originally, then later inverted to slate), removed `<Footer />` from `app/page.tsx`. **(10) See-demo CTA centering bug.** It was flushing left because `.gs-hero` had `align-items: stretch` default; fixed with `align-self: center; width: fit-content; margin: 0 auto` on the button. **(11) EmailGate cramped.** Form max-width was being capped at 560px by `.email-gate`'s own max-width, defeating the `.gs-hero-form` 720px cap. Fixed via `.gs-hero-form .email-gate { max-width: 100% }` override. Then mirrored aiex's `InlineNewsletterSignup` styling: pill-radius (999px), border-2, px-5 py-4 padding, 17px font-size, white surface; bumped form max-width to 540px (input ~360px lands at "relevant length for an email"). **(12) Title bumps** went up and back several times — final clamp(2.5rem, 5vw, 4.25rem) at max-width 18ch. **(13) Cards-stay-anchored fix.** When demo activates, cards used to shift because content above changed height; wrapped the swappable region in a new `.gs-hero-stack` div with `min-height: clamp(420px, 52vh, 560px)` so the stack reserves a fixed vertical region regardless of which content fills it. **(14) Terminal dimensions locked.** Width was jumping because `.gs-demo`'s parent (`.gs-hero-stack` with `align-items: center`) was sizing it to intrinsic content; added explicit `width: 100%` on `.gs-demo` so it fills cross-axis up to 720px from first frame. Replay button now renders from start with `visibility: hidden` until allRevealed so its slot is reserved — eliminates the jump on the last reveal. Terminal body locked at `height: 320px; overflow: hidden`. **(15) Final inversion.** Hero background flipped from grain → primary (white), cards flipped from white → grain (#F0F1F5 slate) so cards read as soft tinted blocks against a clean hero. **(16) Card accessibility.** Body text bumped from 14px → 16px (WCAG body baseline), title 16 → 18, icon 24 → 28, padding 24 → 28, gap 10 → 12. Net: hero is more typographic and quiet, cards have weight without competing with the hero.

### Session 2026-04-27 23:17 (MacBook)
- **Pattern:** Landing page simplification — collapse 4 elaborate flow steps into a 4-card row.
- **Status:** Shipped. Typecheck clean.
- **Files Changed:** 5 modified (web/components/skills/{GetStartedPreview,EmailGate,Nav}.tsx, web/app/skills.css, web/app/get-started/page.tsx).
- **Tests Added/Modified:** 0.
- **Notes:** User compared a designer mock (image #6) against the existing elaborate landing and decided the simpler version was stronger. Consulted dwic itself via `mcp__designwithclaude__design-brief` for a sanity check before rebuilding — verdict came back "ship the simpler version, optionally keep the terminal install mock as one moment of proof; everything else was decorative." Acted on the verdict in three iterations driven by user feedback. **(1) First pass — hero + Get-started pill + 4-card grid, EmailGate moved to `/get-started`.** Rewrote `GetStartedPreview.tsx` from ~250 lines down to ~65: hero (title + sub + dark navy pill button linking to `/get-started`) and a 4-column card grid using Heroicons (CommandLine, Eye, ArrowPath) + DwicIcon for the "learns your project" card. Cards: "Install in Claude Code", "dwic learns your project", "Catch drift on every run", "Monitor continuously" — each ~2 lines of body. Replaced ~770 lines of `.gs-*` mock CSS in `skills.css` (terminal install block, auto-detect summary card, drift dashboard with up/down/flat indicators per category, weekly health card with score + 3 stats + suggestion, two outro sections + their responsive overrides) with ~110 lines for hero + card grid + 2 breakpoints (4→2 cols at 900px → 1 col at 600px). File shrank 3192 → 2420 lines. Modified `/get-started/page.tsx` to render the EmailGate inline when no `?token=` is present (so the Get-started button has a real destination instead of redirecting back to `/`). **(2) User caught the broken loop.** Asked "where is the email gate" — realised `/get-started` was the new destination but I hadn't actually wired it up yet on the first edit; fix was already in place by then but the question prompted me to verify the chain: `/` → click Get started → `/get-started` (no token) → EmailGate form → submit → mint API → `/get-started?token=imr_xxx` install screen. **(3) User changed mind: bring EmailGate back inline + rename submit.** "Lets just have the email block beside the get started and rename to get token instead." Replaced the Get-started pill on `/` with `<EmailGate />` directly (max-width 520px, centered). Renamed submit label "Get my token" → "Get token" in `EmailGate.tsx`. The `/get-started` no-token fallback page stays in place (handles direct visits). **(4) Nav cleanup.** Removed the "Install" link from `Nav.tsx` (was an `#install` anchor that referenced a section ID no longer present after the rebuild). Remaining nav: Free library, AI UX Patterns, GitHub. **Worth logging:** the design-brief consult was useful — gave a clean three-part verdict (under-sell? no; gate placement? move it; keep one mock? terminal install) that I used as the spec, then user iterated against. Plus the master-router prompt comes back as a system message I have to follow, not a synthesized answer — needed to actually generate the response in-character as landing-page-specialist + content-strategist.

### Session 2026-04-27 23:04 (MacBook)
- **Pattern:** Landing-page rebuild on `/` + content/spacing rewrites + `dwic audit --watch` watcher CLI
- **Status:** Shipped end-to-end. Build green, audit-cli-e2e (10 assertions) still green. Watcher live-tested on `web/`.
- **Files Changed:** 13 (2 new — `src/audit/watcher.ts`, `web/components/skills/GetStartedPreview.tsx`; 1 deleted — old `Hero.tsx`).
- **Tests Added/Modified:** 0 (smoke-tested watcher manually; no fixture-based test for fs.watch behavior — would need a chokidar-style harness, deferred).
- **Notes:** Long iterative session driven by user feedback in real time. Three threads. **(1) Landing rebuild on `/`.** Started by reframing `/get-started` from a bare email gate into a transparency-first landing: hero + 4 visual flow steps with inline mocks (mock email, terminal install, auto-detect panel, drift dashboard, weekly health card) + closing email pitch. Then user said "we're repeating ourselves on two pages" — collapsed `/get-started` into `/` itself. Home now renders `<GetStartedPreview />` directly; `/get-started` redirects to `/` when no `?token=`, keeps the post-token install view. Deleted unused `Hero.tsx`. Footer link `/get-started` → `/#install` (anchor on the email form). Full-bleed slate hero band using existing `--background-grain` token (#F0F1F5 light / #162036 dark) — same treatment applied to `/library` hero band so the two pages share visual language. Email form lives once in the hero, repeated as a closing pitch at the bottom of the page. **(2) Copy + spacing rewrites driven by content-strategist + visual-hierarchy-specialist principles.** Headline iterated 5-6 times under user direction: started "From email to drift detection in four steps" → user said "too technical" → "Here's exactly what happens after you give us your email" → user said "unclear" → user dictated "dwic learns from you and guides every design decision" → "remove em dashes" (replaced all em dashes on the landing with periods/colons/commas) → final headline "**dwic is a senior designer inside your terminal**" matching the original brand framing. Sub iterated to "It knows your design system and your design choices. It catches changes before they ship and keeps your system healthy." Section header copy rewritten plain-language throughout — "Install in one command", "dwic gets to know your project", "It spots changes before they ship", "Keeps your system healthy over time". Visual-hierarchy-specialist applied to `/library` spacing: standardized all containers at 1200px (was 960/1200 mix), unified section gaps at 5rem with onboarding (install + learn-cta) tightly bound at 2rem then 5rem break before the directory grid. Library hero subhead trimmed from 55 → 38 words. Content-strategist findings applied to user-visible copy only: dropped "curated" filler, replaced "free / open source" stat with "0 / dependencies" (real datum), converted passive "Skill appears in .claude/commands/" → active "Claude reads the skill from .claude/commands/", reframed "Click any skill card" weak-CTA → "Open any skill card to reveal its install command". Removed redundant "INSTALL COMMAND" label above the curl box. **(3) `dwic audit --watch` — closing the gap between the marketing pitch and reality.** User caught me admitting that dwic doesn't actually watch the codebase as you work, despite the "senior designer in your terminal" framing — said "let's do it now". Built `src/audit/watcher.ts`: native `fs.watch` recursive (macOS/Windows; falls back to shallow on Linux with one-time warning), filters by extension (`.css/.tsx/.jsx/.html/.htm/.ts`), reuses the walker's skip-set so `.dwic/baseline.json` writes don't loop, 300ms debounce coalesces save bursts. Added `--watch / -w` flag to `args.ts` + `--help`. Refactored `src/bin/audit.ts` to extract `runOnePass()` — the first run still emits the full dashboard, then `runWatchLoop()` holds a rolling in-memory baseline, single-flights re-audits (next change while running queues a follow-up), and emits compact one-line drift output: `HH:MM:SS ✓ path no drift` for clean saves, `HH:MM:SS ⚠ path Color ↑1 (1 err) · Typography ↓2 resolved` when drift surfaces. SIGINT / SIGTERM stops cleanly. Live-tested: started watcher on `web/`, touched two files mid-flight, observed two debounced re-audits with correct compact output, then SIGTERM cleanly stopped. Zero new dependencies (chokidar would be cleaner but adds 100KB+; `fs.watch` recursive works on the user's primary macOS dev box). Existing `test:audit-cli-e2e` (10 assertions) still passes — the refactor didn't regress single-shot mode. **Tooltip detour worth logging.** User asked for a designed tooltip on the SkillCard copy area; built CSS `[data-tooltip]::after` pill, then realized parent `.skill-card-expand` has `overflow: hidden` (required by framer-motion's height animation) which clipped the tooltip. Flipped tooltip below the code box. Then user pushed back: "we don't need a tooltip, the Copy word can just have a dark background on hover" — they were right, that's the better UX. Removed all the tooltip CSS, restyled `.skill-card-copy-label` as a proper hover-pill (transparent → dark navy on row hover, brand accent on copied state). Lesson: don't over-engineer affordances that already exist. **Pending follow-ups.** (a) Build + publish a new alpha (`@imrandwc/dwic@1.0.0-alpha.4`?) so the watch flag is available via `npx`; right now it only works from the local repo via `node dist/bin/audit.js`. (b) Consider a fixture-based test for the watcher — would need to mock `fs.watch` events or use a real tmp dir + sleep, both ugly. Defer until the watcher behavior changes meaningfully. (c) Wire `dwic audit --watch` into a `package.json` script suggestion in the post-install guidance. (d) The "What we keep / What you'll get" reassurance section was added to the bottom of `/`, then deleted at user request — content lives in copy elsewhere if needed. (e) Footer "Get started" → `/#install` anchor; when user is mid-page, clicking this scrolls them up to the email form rather than feeling like a page change — verify with eyeballs whether that UX is right or whether a subtle scroll-to-form CTA elsewhere would feel cleaner.

### Session 2026-04-27 19:04 (MacBook)
- **Pattern:** Drift-detection CLI (alpha.4-track) + aiex design-system migration on web + Flow B onboarding (email gate) + orphan cleanup + accessibility-specialist JSX bug fix + dwic-internal email infra (Resend).
- **Status:** Working tree shipped end-to-end. 20/20 audit suites green. Web dev server live, all routes 200, a11y clean per dwic's own audit.
- **Files Changed:** 65 (lots of orphan deletes + targeted new files).
- **Tests Added/Modified:** 5 — `test:audit-drift` (28 assertions), `test:markup-utils` (16), plus `test:audit-dashboard`/`test:audit-cli-e2e` schema-bump updates and `test:phase2-flow` deleted (tested removed wizard).
- **Notes:** Massive multi-thread session anchored on the user's reframing **"dwic is a senior designer that lives in your terminal."** Started with the new positioning ("catches drift before it ships") backed by Anthropic's own admission that semantic sync between Claude Design and Claude Code is unsolved, then built the actual CLI drift engine, then migrated the homepage to aiex's design system, then re-architected the onboarding flow.
  **CLI side (Phase 0 + 1 + telemetry v2 in one commit):** new `src/audit/drift.ts` pure-function diff with stable identity hash on `(category, subject, normalized_message)` — survives line shifts and small numeric jitter (e.g. spacing audit reporting 13px vs 14px doesn't churn as resolved+new). `src/audit/headline-action.ts` picks ONE specialist instead of three for the dashboard CTA (ranked by mandated-drift → severity → category priority where accessibility outranks aesthetics). New CLI flags `--baseline=`, `--no-baseline`, `--mandated-accent=`, `--mandated-font=`, `--base-unit=`. Aggregator extended to wire `mandatedFontFamily` + `baseUnitPx` through to the typography + spacing audits (the wiring already existed in `src/tools/*.ts` but was never plumbed). Telemetry payload bumped to `dwic.audit.summary/2` with drift counts + `headlineCategory` (no rule IDs, no paths). Verified end-to-end on `examples/broken-project`: first run prints "First audit on this project — saving baseline", second unchanged → "no drift since last run", re-run with new `--mandated-accent=#1F3B90` correctly surfaces `↑ 1 new (1 error) · → 28 unchanged`.
  **Web side, aiex migration:** dropped Bevellier serif + DM Mono Google import + Geist; Satoshi-only typography mounted under both `--font-satoshi` AND `--font-dm-sans` aliases so all 1900 lines of `skills.css` keep working without per-class edits. `globals.css` rewritten with aiex's full token set (31 tokens, light-default + reserved `[data-theme="dark"]` block) plus backward-compat aliases (`--text` → `--text-primary`, `--accent` → `--accent-primary`, etc) so the cascade does the heavy lifting. Found a `.skills-page` rule on body that was redefining all the dark tokens at higher specificity — that's why the first paint was navy-on-black; deleted the inline overrides. Hero rewritten to single-page format: navy headline `#162036`, navy pill button, blinking 6px navy cursor (with `prefers-reduced-motion` guard), 4-card "how it works" grid with Heroicons + DwicIcon for "dwic learns your project" card.
  **Flow B onboarding:** killed the 5-step `/start` wizard in favor of post-install onboarding — Anthropic's `/api/profile` POST + onboarding gate already detect framework/Tailwind/tokens at MCP boot and only ask what they can't see (we built that for Cognition months ago; just wasn't the front door). New `/get-started` page with email gate as bot prevention + pre-launch list: single email input → POST `/api/get-started/mint` → `ensureAccount(token, email)` writes to dwic's Supabase profiles → redirect to install screen. Honeypot `website_url` field, RFC-light email validation, graceful Resend integration with `RESEND_API_KEY` env var (no-op if missing).
  **Orphan deletion:** removed 6 routes (`/start`, `/profile`, `/install`, `/companion`, `/upgrade`, `/account`) + ~12 orphan-only components (entire `components/companion/` directory + `Shell` + `InstallV2` + `AuditDemo` + `SpecialistsList`) + obsolete `test:phase2` script. All API routes preserved (still hit by MCP server). One Footer link rewired from `/start` → `/get-started`. Deletion was blocked initially by stale `.next/types` stubs; cleared and rebuild fixed it.
  **Accessibility-specialist core bug:** discovered while auditing the new EmailGate that `[^>]*` in the input regex stops at the first `>` character, which means JSX `onChange={(e) => fn()}` makes the regex think the tag ended before `aria-label` could be detected. Fixed in `src/tools/markup-utils.ts` (new module) with `neutralizeJsxExpressions()` — strips JSX expression `{...}` blocks while preserving function bodies / TS types / object literals at non-JSX positions (lookback rule: only collapse `{` if preceded by `=` or `>`). Wired into all 4 markup-based audits (`accessibility`, `form`, `navigation`, `content`). Initial implementation was too aggressive and ate function bodies entirely — caught by re-running the audit on `app/get-started/page.tsx` and noticing `<main>` had vanished from the cleaned markup. 16 unit-test assertions cover the edge cases: arrow functions, nested braces, string literals, template literals with interpolation, comparison operators in expressions, escaped quotes, unclosed expressions, real-world JSX inputs.
  **dwic email infra:** removed `<NewsletterSignup />` from `/library` (it forwarded emails to aiuxdesign.guide — the explicit data-mixing the user wanted gone), deleted the unused component + `/api/subscribe` proxy. Built `web/lib/dwic/email.ts` with direct-fetch Resend integration (no new npm dep), `sendInstallEmail()` template (HTML + plain-text, table-layout for Outlook/Gmail compat, navy aiex palette). Wired into `/api/get-started/mint` as fire-and-forget — never blocks the redirect, returns `emailQueued: boolean` so the client knows whether mail went out. Graceful no-op when `RESEND_API_KEY` is missing.
  **Pending env vars for production email delivery:** `RESEND_API_KEY` + (optional) `DWIC_FROM_EMAIL`. Plus the `002_email.sql` migration to add `profiles.email` column — must run before deploying or Supabase upserts will fail on missing column.

### Session 2026-04-22 evening (MacBook)
- **Pattern:** Shipped alpha.3 working tree — `dwic audit` CLI. Zero-friction one-liner that scans a project, prints an 8-category dashboard, writes a shareable markdown report, pings anonymous telemetry so we can measure CLI → MCP funnel.
- **Status:** Code-complete on `audit-cli` branch. 20 test suites green. Publish + screencast pending.
- **Files Changed:** 15 new (src/bin/audit.ts, src/audit/{args,walker,aggregator,dashboard,markdown-report,telemetry}.ts, examples/broken-project/{package.json,src/styles/themes.css,src/styles/motion.css,src/components/Signup.tsx}, ROADMAP_AUDIT_CLI.md, 5 test scripts), 3 edited (src/bin/setup.ts for subcommand dispatch, package.json bin+scripts+version, README.md top-level CTA, PROGRESS.md + CLAUDE.md session notes).
- **Tests Added/Modified:** 5 new — test:audit-walker (8), test:audit-aggregator (16), test:audit-dashboard (12), test:audit-markdown-report (7), test:audit-cli-e2e (~8). Full regression sweep passing (all 20 suites).
- **Notes:** Kicked off with the advisor frame from user: "pick the single strongest skill — the auditor framing — and turn it into a real tool with a demo." Planned in plan mode, executed in auto mode. **Architecture bet:** the CLI and the MCP share the same audit brain — every `runXAudit()` is already a pure function (the whole reason we factored helpers out of the tool wrappers in batches 2 + 3). The CLI is ~350 new lines of composition on top of existing helpers. **Walker design:** sync walker with skip-set (`node_modules`/`.next`/`dist`/`.git`/etc.), prioritises high-signal CSS files (themes.css, tokens.css) so token parsers see the designer's canonical surface ahead of framework preflight. JSX/TSX files pre-filtered by regex for opening elements — keeps hooks/utils out of a11y audit noise. Cap at 200 markup files with cap-notice in the dashboard. **Dashboard rules:** hand-rolled ANSI (no `chalk` dep — keeps deps flat), respects NO_COLOR + non-TTY, severity-sorted rows (errors first, clean last), up to 3 MCP follow-up suggestions grounded in actual findings. Exit code mirrors severity (0/1/2) for CI gates. **Telemetry contract:** first-run opt-out notice printed once (stored in `~/.dwic/state.json::telemetryNoticeShown`), anonymous client ID minted via `crypto.randomBytes(6).toString('base64url')`, payload is schema-versioned (`dwic.audit.summary/1`) and PII-free (framework + category counts + totals only). `--no-telemetry` + `DWIC_TELEMETRY=off` both disable. 2s timeout, silent failure — never blocks exit. **Two bugs caught during smoke:** (1) dashboard header used `process.cwd()` instead of the `--cwd=` arg; fixed by threading `cwd` through DashboardOptions. (2) form regex `/\b<(form|input|...)\b/i` didn't match because `\b` before `<` fails when the preceding char is also non-word; dropped the leading `\b` and the fixture's form findings surfaced correctly. **Fixture design:** `examples/broken-project/` is a real Next.js 15 + Tailwind v4 project with planted findings in every audit category. Output: 28 findings, 6 errors, 14 warns, 8 info, exit 2. Same fixture powers the e2e test AND the screencast target. **Open:** npm publish needs a fresh automation token per the rotation policy (revoked yesterday); user to cut the 60s screencast before distribution. **Decision point baked into the plan:** after 7 days of launch telemetry, either double down on the auditor thesis or reassess if the 45-skills library framing was right all along.

### Session 2026-04-22 (MacBook)
- **Pattern:** Rename cleanup + alpha.2 — audit batch 3 + three Cognition-roadmap items (C9 slice 3 + C10 slice 2 + C1) in one publish.
- **Status:** Code-complete in working tree. 17 test suites green. Publish pending user sign-off.
- **Files Changed:** 18 — 6 modified (package.json, src/server.ts, src/designer.ts, src/tools/index.ts, scripts/test-memory-context.mjs, scripts/test-explicit-asks.mjs, PROGRESS.md, CLAUDE.md), 10 new (src/tools/{content,content-strategist,motion,motion-designer,design-next-step}.ts, src/utils/detect-project-config.ts, commands/design-next-step.md, scripts/test-audit-{content-strategist,motion-designer}.mjs, scripts/test-{detect-project-config,design-next-step}.mjs).
- **Tests Added/Modified:** 4 new test scripts — test:audit-content-strategist (17), test:audit-motion-designer (18), test:detect-project-config (21), test:design-next-step (15). test:memory-context extended by 7 assertions. test:explicit-asks extended with fixtures for the three new tools.
- **Notes:** Day opened with rename cleanup — verified `npx @imrandwc/dwic@latest help` from clean tmp dir (✓), then revoked the npm automation token on the web and cleared it from `~/.npmrc` (both checklist items on PROGRESS.md ticked). Then user chose to keep building before committing. **Audit batch 3:** content-strategist (copy heuristics: weak CTAs, jargon/filler list, passive voice, >30-word sentences, ALL CAPS shouting outside known acronyms, Title Case headings) + motion-designer (CSS motion: missing reduced-motion → **error**, duration bounds >1000ms/<80ms, `transition: all`, animated layout props like top/width force reflow, infinite animations without guard → warn else info, majority-linear easing). Both follow the established batch-2 pattern (`<name>.ts` helpers + `<name>-specialist/designer.ts` tool definition + composeRolePrompt audit-mode closing instruction that forbids fresh generation). **C9 slice 3 (auto-detect):** green-field utility — no existing filesystem reading. `detectProjectConfig(cwd)` parses `package.json` (framework via dependency priority: Next → Remix → Astro → Nuxt → SvelteKit → Expo → RN → Vite), walks common CSS locations for `--color-*` + `--space-*` tokens, sanitizes `package.json.name` into a slug candidate, returns `{ framework, techStack[], designSystemHints[], slug, confidence: "high|partial|none" }`. Wired into server.ts main(): runs once on boot (not per-tool-call) and threads into `registerTool` ctx. `renderOnboardingResponse` now accepts the detection and prepends a "We detected..." block above the 6 questions. Fail-open by design — any file-read error drops to confidence `none` and the response falls back to the original 6 questions. **C10 slice 2 (richer gists):** `describeEvent` in designer.ts expanded to mine payload data — palette audit mode compares `input.accent` against parsed tokens' hex to report `mandated #1F3B90 present|MISSING`, markdown events parse the `Flagged N error(s), M warning(s), P info note(s)` line every audit renderer emits (yields `(audit) 3 error(s), 2 warn(s), 1 info` as a real gist), every gist capped at 120 chars with ellipsis. Zero payload-shape changes, zero server-side work — all inferred from existing `output.data` + `input`. **C1 (design-next-step):** new MCP tool that synthesizes profile + recent-events into a single named recommendation. `src/tools/design-next-step.ts` is ~60 lines — iterates `tools[]` for the roster (minus self/hello-world/set-project-profile), composes a role prompt from new `commands/design-next-step.md`. Optional `focus` input biases recommendation. Role prompt mandates ONE specialist, exact MCP tool call format, "Why not something else" falsifiability clause. Handler accepts empty input — designer just calls `design-next-step()` with nothing. **test:explicit-asks plumbing update:** needed a SKIP_BRIEF_ECHO set because design-next-step has no `brief` field; the C3 directive + response-shape check still applies, just not the brief-restatement sanity check. **No decisions left hanging** — all three items shipped end-to-end with tests; next action is commit + publish dance.

### Session 2026-04-21 afternoon (MacBook)
